// Social / discovery data layer.
//
// Populated in Phase 8: getPeopleToFollow(userId, limit) — users the current user
// doesn't follow yet (most-followed or most-recently-active, excluding self).

export {};
