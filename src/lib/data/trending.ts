// Trending data layer.
//
// Populated in Phase 5: getTrending({ window, scope }) — books ranked by recent
// activity (count of finished + started_reading events, and/or TBR adds, in the
// window). v1 is a simple app-wide count, NOT the parked recommendation engine.

export {};
