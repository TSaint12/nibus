// Reading lifecycle data layer.
//
// Populated in Phase 4 (migrated from src/app/reading/actions.ts): startRead,
// updateProgress (QUIET — emits no event now), finishRead, logFinishedRead,
// getCurrentlyReading, abandonBook. Only started_reading + finished emit feed
// events. Client-callable via the browser Supabase client.

export {};
